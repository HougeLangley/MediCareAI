FROM node:18-alpine

WORKDIR /app

# 安装依赖（绕过缓存，强制安装）
RUN npm cache clean --force
COPY package*.json ./
RUN npm install --legacy-peer-deps --no-optional && npm cache clean --force

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "start"]